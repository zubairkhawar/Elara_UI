# Copy this file to .env in the backend folder, then fill in values.
# See backend/docs/GMAIL_EMAIL_SETUP.md for Gmail App Password setup.

DEBUG=True
SECRET_KEY=dev-secret-key-change-me
ALLOWED_HOSTS=localhost,127.0.0.1,*

DB_NAME=elara
DB_USER=elara
DB_PASSWORD=elara
DB_HOST=localhost
DB_PORT=5432

# Gmail: set both to send "your account is ready" when you activate a user in admin.
# 1) Google Account → Security → 2-Step Verification → App passwords → Generate.
# 2) Put your Gmail and the 16-char app password below.
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
